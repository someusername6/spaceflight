# Development environment variables
# These are used when running `npm run dev` or `npm run build -- --mode development`

# Local signaling server for multiplayer testing
VITE_SIGNALING_URL=http://localhost:3001
